apiVersion: v1
kind: Service
metadata:
  name: electronics-service
  labels:
    app: electronics
spec:
  selector:
    app: electronics
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
